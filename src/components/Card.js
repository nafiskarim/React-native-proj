import React from 'react'
import { View } from 'react-native'

const Card () => {
  return(

  )
}

const styles ={
  containerStyle> {
    borderWidth: 1,
    borderRaius: 2,
    borderColor: '#ddd'
  }
}

export default Card
